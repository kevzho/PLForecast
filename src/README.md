# Overview of `src` subfolder

---
### Common misinterpretations:

`data_loader.py` and `load_data.py` are different - `data_loader.py` loads power rankings and current league table and are inputs for simulation. `load_data.py` is for loading raw match results from CSV, which was used for initial data prep. 